services:
  - type: web
    name: scorton-gtm-staging
    runtime: python
    branch: feature/email-verification
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT --log-level debug
